// Where the oRPC endpoint is mounted — the client-side client, the API route
// serving it (src/routes/api/rpc/[...rest].ts), and external clients share
// this path.
export const rpcPath = "/api/rpc";
